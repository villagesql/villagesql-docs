---
title: "Development"
description: "Writing and running tests for your VillageSQL extension."
---

This guide covers writing and running regression tests for VillageSQL extensions using the `./villagesql mysql-test` wrapper.

<Note>
  If you are contributing to the VillageSQL server itself (not building an extension), see [Clone and Build from Source](/mysql-8.4/0.0.3-dev/source), which covers the full server developer workflow including running tests with `mysql-test-run.pl` directly.
</Note>

## Setting Up Your Environment

To run extension tests, you need a built VillageSQL server. Follow the [Clone and Build from Source](/mysql-8.4/0.0.3-dev/source) guide to compile the server binaries.

Once you have a build directory (e.g., `build/`), you are ready to run regression tests for your extension.

## Writing Extension Functions

### Type Operation Builders

Custom types require four operations: encode (string → binary), decode
(binary → string), compare, and hash. Implement them against these
ergonomic C++ signatures (all defined in `func_builder.h`):

```cpp
// Encode: string -> binary. false = success, true = error.
// Set *length = SIZE_MAX to produce SQL NULL.
using TypeEncodeFunc = bool (*)(std::string_view from,
                                villagesql::Span<unsigned char> buf,
                                size_t *length);

// Decode: binary -> string. false = success, true = error.
using TypeDecodeFunc = bool (*)(villagesql::Span<const unsigned char> data,
                                villagesql::Span<char> out,
                                size_t *out_len);

// Compare: returns <0, 0, or >0 (used for ORDER BY and indexes).
using TypeCompareFunc = int (*)(villagesql::Span<const unsigned char> a,
                                villagesql::Span<const unsigned char> b);

// Hash: returns hash code (used for hash joins).
using TypeHashFunc = size_t (*)(villagesql::Span<const unsigned char> data);
```

Register each operation with `make_type_encode`, `make_type_decode`,
`make_type_compare`, or `make_type_hash`, passing the function pointer and
the type name constant. The type descriptor references them by name:

```cpp
constexpr const char *MYTYPE = "mytype";

VEF_GENERATE_ENTRY_POINTS(
    make_extension("my_ext", "1.0.0")
        .type(make_type(MYTYPE)
                  .persisted_length(8)
                  .max_decode_buffer_length(64)
                  .encode("mytype_encode")    // Name of the encode VDF
                  .decode("mytype_decode")    // Name of the decode VDF
                  .compare("mytype_compare")  // Name of the compare VDF
                  .hash("mytype_hash")        // Optional: name of the hash VDF
                  .build())
        .func(make_type_encode<&my_encode>("mytype_encode", MYTYPE))
        .func(make_type_decode<&my_decode>("mytype_decode", MYTYPE))
        .func(make_type_compare<&my_compare>("mytype_compare", MYTYPE))
        .func(make_type_hash<&my_hash>("mytype_hash", MYTYPE)))
```

### Parameterized Types

Custom types can accept parameters (e.g., `TVECTOR(1536)`). The server calls
two VDFs during DDL to translate the integer argument into storage
characteristics:

1. **int_to_params** — converts the integer (e.g., `1536`) into a
   key-value map (e.g., `{"dimension": "1536"}`).
2. **resolve_params** — validates the map and computes `persisted_length`
   and `max_decode_buffer_length`.

Write against `IntToTypeParamsFunc` and `ResolveTypeParamsFunc` (both in
`func_builder.h`); the SDK wraps them into VDFs automatically:

```cpp
using IntToTypeParamsFunc = bool (*)(
    int64_t value,
    std::map<std::string, std::string> &params,
    char *error_msg);  // false = success, true = error

using ResolveTypeParamsFunc = bool (*)(
    const std::map<std::string, std::string> &params,
    villagesql::ResolvedTypeParams *result,  // {persisted_length, max_decode_buffer_length}
    char *error_msg);  // false = success, true = error
```

Register with `make_int_to_params` / `make_resolve_params`, then bind the VDF
names via `TypeBuilder::int_to_params()` / `TypeBuilder::resolve_params()`.

**Complete example** — TVECTOR(N) stores N float32 elements:

```cpp
#include <villagesql/extension.h>

#include <cstdint>
#include <map>
#include <string>

bool tvector_int_to_params(int64_t value,
                           std::map<std::string, std::string> &params,
                           char *error_msg) {
  if (value <= 0) {
    snprintf(error_msg, VEF_MAX_ERROR_LEN,
             "TVECTOR dimension must be positive, got %" PRId64, value);
    return true;
  }
  params["dimension"] = std::to_string(value);
  return false;
}

bool tvector_resolve_params(
    const std::map<std::string, std::string> &params,
    villagesql::ResolvedTypeParams *result,
    char *error_msg) {
  auto it = params.find("dimension");
  if (it == params.end()) {
    snprintf(error_msg, VEF_MAX_ERROR_LEN,
             "TVECTOR: missing 'dimension' parameter");
    return true;
  }
  int64_t dim = strtoll(it->second.c_str(), nullptr, 10);
  if (dim <= 0) {
    snprintf(error_msg, VEF_MAX_ERROR_LEN,
             "TVECTOR: dimension must be positive");
    return true;
  }
  result->persisted_length = dim * 4;
  result->max_decode_buffer_length = dim * 16;
  return false;
}

constexpr const char *TVECTOR = "TVECTOR";

VEF_GENERATE_ENTRY_POINTS(
    make_extension("vsql_tvector", "0.0.1")
        .type(make_type(TVECTOR)
                  .persisted_length(-1)
                  .max_decode_buffer_length(16)
                  .encode("tvector_from_string")
                  .decode("tvector_to_string")
                  .compare("tvector_compare")
                  .int_to_params("tvector_int_to_params")
                  .resolve_params("tvector_resolve_params")
                  .build())
        // ... encode/decode/compare VDFs omitted for brevity ...
        .func(make_int_to_params<&tvector_int_to_params>(
            "tvector_int_to_params"))
        .func(make_resolve_params<&tvector_resolve_params>(
            "tvector_resolve_params")))
```

`persisted_length(-1)` signals variable storage; `resolve_params` supplies the
actual length at DDL time.

### Intrinsic Defaults

When a `NOT NULL` custom column receives `NULL` via `INSERT IGNORE` or
`UPDATE IGNORE`, the server calls the type's intrinsic default to produce a
fallback binary value. Without one, the server rejects the row.

Fixed-size types use this signature:

```cpp
// false = success, true = error
using IntrinsicDefaultFunc = bool (*)(
    villagesql::Span<unsigned char> buffer,
    size_t *length,
    char *error_msg);
```

Variable-size types that need type parameters (e.g., `TVECTOR(N)`) use:

```cpp
using IntrinsicDefaultWithParamsFunc = bool (*)(
    const std::map<std::string, std::string> &params,
    villagesql::Span<unsigned char> buffer,
    size_t *length,
    char *error_msg);
```

The SDK detects which signature you implement at compile time — write
whichever matches your type.

Register with `make_intrinsic_default<&func>("name", TYPE)`, bind via
`TypeBuilder::intrinsic_default()`.

**Complete example** — COMPLEX defaults to `(0,0)`:

```cpp
constexpr size_t kComplexSize = 16;  // two doubles

bool complex_default(villagesql::Span<unsigned char> buffer,
                     size_t *length, char * /*error_msg*/) {
  if (buffer.size() < kComplexSize) {
    *length = 0;
    return true;
  }
  Complex cx{0.0, 0.0};
  store_complex(buffer.data(), cx);
  *length = kComplexSize;
  return false;
}

VEF_GENERATE_ENTRY_POINTS(
    make_extension("vsql_complex", "0.0.1")
        .type(make_type(COMPLEX)
                  .persisted_length(kComplexSize)
                  .max_decode_buffer_length(64)
                  .encode("complex_from_string")
                  .decode("complex_to_string")
                  .compare("complex_compare")
                  .intrinsic_default("complex_intrinsic_default")
                  .build())
        // ... other VDFs omitted for brevity ...
        .func(make_intrinsic_default<&complex_default>(
            "complex_intrinsic_default", COMPLEX)))
```

## Running Regression Tests

VillageSQL uses the `./villagesql mysql-test` wrapper for regression testing. This command passes arguments through to the MySQL Test Framework. Run all commands below from your VillageSQL dev server directory.

### Running the Full Suite

To run all tests for your extension:

```bash
./villagesql mysql-test --suite=/path/to/your/extensions/tests --parallel=auto
```

### Running Individual Tests

To run a single test case, specify the suite path and test name:

```bash
# Run a specific insert test
./villagesql mysql-test villagesql/insert.insert_values_complex
```

## Creating New Tests

When adding new features or fixing bugs, you should add corresponding regression tests.

### Test Location

Tests are located in `mysql-test/suite/villagesql/`. Choose the appropriate sub-directory based on the SQL statement or feature being tested.
- Test files end with `.test` and go in the `t/` directory.
- Expected result files end with `.result` and go in the `r/` directory.

### Test File Conventions

VillageSQL tests often involve the custom `COMPLEX` type. We follow a specific pattern to ensure tests are skipped gracefully if the feature is not yet fully implemented:

```sql
# Description of the test
# Grammar: (Optional) SQL syntax reference

--source include/villagesql/not_implemented_complex_type.inc

--source include/villagesql/install_complex_extension.inc

# ... Your Test Code Here ...
CREATE TABLE t1 (val COMPLEX);
INSERT INTO t1 VALUES ('(1.0,2.0)');
SELECT * FROM t1;
DROP TABLE t1;

--source include/villagesql/uninstall_complex_extension.inc
```

### Steps to Add a Test

1.  **Create the `.test` file** in the appropriate `t/` directory (e.g., `mysql-test/suite/villagesql/select/t/my_new_test.test`).
2.  **Create an empty `.result` file** in the corresponding `r/` directory (e.g., `mysql-test/suite/villagesql/select/r/my_new_test.result`).
3.  **Run the test with `--record`** to generate the expected output:
    ```bash
    ./villagesql mysql-test --suite=villagesql/select --record my_new_test
    ```
4.  **Verify the output** in the generated `.result` file to ensure it matches your expectations.

## Debugging Tests

If a test fails, the test framework provides detailed logs.

-   **Test output:** Check `mysql-test/var/log/my_new_test.log`.
-   **Server error log:** Check `mysql-test/var/log/mysqld.1.err`.
-   **Diff:** The framework outputs a diff between the actual output and the expected `.result` file.

To run a test with extra debug information:

```bash
./villagesql mysql-test --verbose villagesql/select.my_new_test
```
