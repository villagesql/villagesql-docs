---
title: "创建自定义类型"
description: "为 VillageSQL 扩展定义新列类型 — 类型操作、ALTER TABLE 规则、转换函数，以及完整的 COMPLEX 类型示例。"
---

<Warning>
  自定义类型使用 VEF 协议 3，自 v0.0.4 起稳定。协议 4 正在开发中，仅通过 opt-in dev ABI 头文件（`-DVSQL_USE_DEV_ABI=ON`）可用。针对旧协议 2 构建的扩展将被服务器拒绝，必须重新构建。
</Warning>

自定义类型允许您定义新列类型 — 如 `COMPLEX`、`UUID` 或 `VECTOR` — 这些类型可与 `ORDER BY`、索引和聚合函数一起使用。本页是[创建扩展](/mysql-8.4/0.0.4/create)教程的第 4 步。在继续之前，请完成第 1–3 步。

## 定义类型操作

每个自定义类型都需要编码、解码和比较操作，可选地包含哈希操作。按照以下签名实现它们，并将构建器对象传递给 `vsql::make_type<>()`：

```cpp
// Encode: string -> binary. Write to out.buffer() and call out.set_length(n).
void mytype_from_string(std::string_view from, vsql::CustomResult out) { /* ... */ }

// Decode: binary -> string. Write to out.buffer() and call out.set_length(n).
void mytype_to_string(vsql::CustomArg in, vsql::StringResult out) { /* ... */ }

// Compare: returns <0, 0, or >0.
int mytype_compare(vsql::CustomArg a, vsql::CustomArg b) { /* ... */ }

// Hash: returns hash code (optional).
size_t mytype_hash(vsql::CustomArg in) { /* ... */ }
```

<Note>
对于返回自定义类型的 `from_string` VDF（值转换函数），服务器在调用 VDF 前将输出缓冲区大小设置为至少等于类型 `persisted_length` 值，因此在入口处保证 `buf.size() >= persisted_length`。这对固定宽度类型和参数化类型（其中 `persisted_length` 在调用时从类型上下文解析）均适用。无需单独请求缓冲区大小。
</Note>

原始二进制访问通过 `vsql::Span<T>` 进行，这是一个非拥有视图，覆盖连续的 `T` 序列 — `in.value()` 返回 `vsql::Span<const unsigned char>`，`out.buffer()` 返回 `vsql::Span<unsigned char>`。使用 C++20 或更高版本时，`vsql::Span<T>` 是 `std::span<T>` 的别名；在 C++17 下，SDK 提供了最小兼容的回退实现，具有相同的 `data()`、`size()`、`empty()`、索引和迭代器接口。可通过 `#include <villagesql/vsql.h>` 获取。

## 注册类型

`vsql::make_type<kName>()` 模板将编码、解码、比较和哈希操作直接嵌入类型对象中。VDF 名称在编译时自动生成为 `TYPE::from_string`、`TYPE::to_string`、`TYPE::compare` 和 `TYPE::hash`。无需单独的 `.func(make_type_encode<>(...))` 调用。

```cpp
#include <villagesql/vsql.h>

using namespace vsql;

// Required for auto-generating VDF names at compile time.
static constexpr const char kMyTypeName[] = "MYTYPE";

constexpr auto MYTYPE =
    vsql::make_type<kMyTypeName>()
        .persisted_length(16)
        .max_decode_buffer_length(64)
        .from_string<&mytype_from_string>()   // auto: "MYTYPE::from_string"
        .to_string<&mytype_to_string>()       // auto: "MYTYPE::to_string"
        .compare<&mytype_compare>()           // auto: "MYTYPE::compare"
        .hash<&mytype_hash>()                 // optional; auto: "MYTYPE::hash"
        .intrinsic_default_str("...")         // must encode to exactly 16 bytes; see Development guide
        .build();

VEF_GENERATE_ENTRY_POINTS(
    make_extension()
        .type(MYTYPE)
);
```

如果缺少 `from_string`、`to_string` 或 `compare`，`build()` 将无法编译。每个模板方法通过 `static_assert` 验证函数指针签名。

类型名称作为非类型模板参数（NTTP）传递。将其声明为 `static constexpr const char[]` 数组 — 指针身份用于键入独立的 VDF 名称缓冲区，因此两个类型共享函数指针时仍会获得单独自动生成的名称。

## 类型操作参考

基于模板的 API 自动生成以下 SQL 可调用 VDF：

| 构建器方法 | 自动生成的 VDF 名称 | VDF SQL 签名 |
|------|------|-------------|
| `.from_string<&f>()` | `TYPE::from_string` | `(STRING) -> CUSTOM(this type)` |
| `.to_string<&f>()` | `TYPE::to_string` | `(CUSTOM(this type)) -> STRING` |
| `.compare<&f>()` | `TYPE::compare` | `(CUSTOM(this type), CUSTOM(this type)) -> INT` |
| `.hash<&f>()` | `TYPE::hash` | `(CUSTOM(this type)) -> INT` |

有关完整 C++ 签名，请参阅[类型操作构建器](/mysql-8.4/0.0.4/development#type-operation-builders)。

## ALTER TABLE 与自定义类型

当涉及自定义类型时，`ALTER TABLE ... MODIFY COLUMN` 和 `CHANGE COLUMN` 强制执行以下规则：

| 来源 | 目标 | 结果 |
|------|----|------|
| 非自定义类型 | 自定义类型 | 错误：`Cannot convert column 'col' to custom type 'MYTYPE'` |
| 自定义类型 | 字符串类型 | 允许 |
| 自定义类型 | 非字符串类型 | 错误：`Cannot convert custom type column 'col' to non-string type` |
| 自定义类型 | 不同的自定义类型 | 不兼容时出错：`Cannot convert between incompatible custom types 'A' and 'B'` |

## 类型转换函数

使用基于模板的 API 时，编码和解码 VDF 已嵌入类型对象并自动注册 — 无需单独的 `.func()` 调用。自动生成的 VDF 是 SQL 可调用的：

```sql
-- Convert string to custom type (calls MYTYPE::from_string)
SELECT MYTYPE::from_string('(1.0,2.0)');

-- Convert custom type to string (calls MYTYPE::to_string)
SELECT MYTYPE::to_string(my_column) FROM my_table;

-- Explicit conversion in INSERT
INSERT INTO my_table (id, value)
VALUES (1, MYTYPE::from_string('(3.0,4.0)'));
```

**需要显式转换时。** VillageSQL 在直接列赋值时隐式将字符串字面量转换为自定义类型，因此 `INSERT INTO t (val) VALUES ('(1.0,2.0)')` 无需显式调用即可工作。但解析为 `STRING` 类型的表达式 — `CASE` 表达式、`CONCAT` 等 — 不会隐式强制转换。请使用 `TYPE::from_string` 包裹它们：

```sql
UPDATE my_table
SET val = MYTYPE::from_string(
  CASE (pk MOD 2)
    WHEN 0 THEN '(1.0,2.0)'
    ELSE '(0.0,0.0)'
  END
);
```

## 示例：COMPLEX 类型

以下是实现 COMPLEX 数字类型的完整示例：

```cpp
#include <cstdio>
#include <cstring>
#include <villagesql/vsql.h>

using namespace vsql;

// Encode: "(real,imag)" string -> 16 bytes little-endian
void encode_complex(std::string_view from, CustomResult out) {
    auto buf = out.buffer();
    if (buf.size() < 16) return;
    double real, imag;
    if (sscanf(from.data(), "(%lf,%lf)", &real, &imag) != 2) {
        out.warning("invalid complex format: expected (real,imag)");
        return;
    }
    memcpy(buf.data(), &real, 8);
    memcpy(buf.data() + 8, &imag, 8);
    out.set_length(16);
}

// Decode: 16 bytes -> "(real,imag)" string
void decode_complex(CustomArg in, StringResult out) {
    auto data = in.value();
    if (data.size() < 16) return;
    double real, imag;
    memcpy(&real, data.data(), 8);
    memcpy(&imag, data.data() + 8, 8);
    auto buf = out.buffer();
    int len = snprintf(buf.data(), buf.size(), "(%.6f,%.6f)", real, imag);
    if (len < 0 || static_cast<size_t>(len) >= buf.size()) return;
    out.set_length(static_cast<size_t>(len));
}

// Compare for ORDER BY: real part first, then imaginary
int compare_complex(CustomArg a, CustomArg b) {
    auto da = a.value();
    auto db = b.value();
    if (da.size() < 16 || db.size() < 16) return 0;
    double a_real, a_imag, b_real, b_imag;
    memcpy(&a_real, da.data(), 8);
    memcpy(&a_imag, da.data() + 8, 8);
    memcpy(&b_real, db.data(), 8);
    memcpy(&b_imag, db.data() + 8, 8);
    if (a_real < b_real) return -1;
    if (a_real > b_real) return 1;
    if (a_imag < b_imag) return -1;
    if (a_imag > b_imag) return 1;
    return 0;
}
```

定义这些操作后，用户可以使用您的自定义类型创建表：

```sql
CREATE TABLE signals (
    id INT PRIMARY KEY,
    impedance COMPLEX,
    frequency_response COMPLEX
);

INSERT INTO signals VALUES (1, '(50.0,10.0)', '(0.95,0.31)');

-- ORDER BY works because we provided compare_complex!
SELECT * FROM signals ORDER BY impedance;

-- Prepared statements work with custom types
PREPARE stmt FROM 'SELECT * FROM signals WHERE impedance = ?';
SET @val = '(50.0,10.0)';
EXECUTE stmt USING @val;

-- Aggregate operations work with custom types
SELECT COUNT(DISTINCT impedance), MIN(impedance), MAX(impedance),
       GROUP_CONCAT(impedance ORDER BY impedance) FROM signals;
```

## 生成列中的 VDF

VDF 可用于生成列表达式。VDF 必须在扩展构建器中声明为 `.deterministic()` — 服务器会阻止此上下文中非确定性函数。

```sql
CREATE TABLE signals (
    id INT PRIMARY KEY,
    impedance COMPLEX,
    -- Generated column computed by a VDF
    magnitude DOUBLE GENERATED ALWAYS AS (complex_abs(impedance)) STORED
);
```

<Note>
`complex_abs` 必须通过 `.deterministic()` 注册。传统 MySQL UDF 不允许用于生成列。
</Note>

有关完整实现，请参阅[vsql_complex 示例](/mysql-8.4/0.0.4/examples)。

## 功能索引中的 VDF

VDF 可用于功能索引表达式。与生成列相同，此处也要求 `.deterministic()`，因为 MySQL 将功能索引实现为隐藏生成列。

```sql
CREATE TABLE signals (
    id INT PRIMARY KEY,
    sig COMPLEX,
    INDEX idx_magnitude ((COMPLEX_ABS(sig)))
);
```

优化器在 `WHERE`、`ORDER BY` 或 `GROUP BY` 中出现相同 VDF 表达式时使用索引。将比较值转换为 VDF 返回类型，使优化器匹配表达式：

```sql
SELECT id FROM signals WHERE COMPLEX_ABS(sig) > CAST(20.0 AS DOUBLE);
```

## 下一步

定义类型后，继续教程的第 5 步以构建和安装您的扩展。

<CardGroup cols={2}>
  <Card
    title="继续：构建您的扩展"
    icon="hammer"
    href="/mysql-8.4/0.0.4/create#step-5-update-build-configuration"
  >
    返回教程以构建和安装您的扩展。
  </Card>
  <Card
    title="参数化类型"
    icon="sliders"
    href="/mysql-8.4/0.0.4/development#parameterized-types"
  >
    支持参数的类型，如 VECTOR(1536) — 维度感知的编码、解码和存储大小。
  </Card>
  <Card
    title="扩展 API 参考"
    icon="book"
    href="/mysql-8.4/0.0.4/extension-api-reference"
  >
    VDF API 合约、空值处理、缓冲区大小和高级模式。
  </Card>
  <Card
    title="复制"
    icon="arrow-right-left"
    href="/mysql-8.4/0.0.4/managing#replication"
  >
    ROW 格式要求、扩展安装顺序和复制设置中的版本匹配。
  </Card>
</CardGroup>