---
title: "服务器开发指南"
description: "调试、测试和贡献到 VillageSQL 服务器代码库。"
---

本指南涵盖 GDB 调试、运行服务器测试套件以及贡献到 VillageSQL。请先通过 [从源码构建](/mysql-8.4/0.0.4/source) 获取可工作的构建，再使用本指南。

## 使用 GDB 调试

在开发和故障排除时，您可以在 GDB 下运行 VillageSQL：

**Linux:**
```bash
gdb --args $HOME/build/villagesql/bin/mysqld --gdb --datadir=$HOME/mysql-data/data --basedir=$HOME/build/villagesql
```

**macOS:**
```bash
gdb --args ~/build/villagesql/bin/mysqld --gdb --datadir=~/mysql-data/data --basedir=~/build/villagesql
```

**在 GDB 内部:**
```
(gdb) run                    # Start the server
(gdb) break function_name    # Set breakpoints
(gdb) continue              # Resume execution
(gdb) bt                    # Show backtrace on crash
```

**MySQL 调试常用 GDB 命令:**
```
# Break on specific SQL command execution
(gdb) break mysql_execute_command

# Break on extension loading
(gdb) break Sql_cmd_install_extension::execute

# Print variable values
(gdb) print variable_name

# Step through code
(gdb) step      # Step into functions
(gdb) next      # Step over functions
```

## 运行测试

VillageSQL 包含单元测试和回归测试以验证功能。

### 单元测试

运行 VillageSQL 特定的单元测试（在构建目录内）：

```bash
make -j10 villagesql-unit-tests && ctest -L villagesql
```

或运行所有单元测试：

```bash
ctest --output-on-failure
```

### 回归测试

切换到 mysql-test 目录：
```bash
cd mysql-test
```

**运行完整的 MySQL 测试套件（较慢）:**
```bash
./mysql-test-run.pl --parallel=auto
```

**仅运行 VillageSQL 特定测试:**
```bash
./mysql-test-run.pl --suite=villagesql
```

**运行包含所有子套件的 VillageSQL 测试:**
```bash
./mysql-test-run.pl --do-suite=villagesql --parallel=auto
```

<Note>
  **--suite 与 --do-suite:**
  - `--suite=villagesql` 仅运行顶级 villagesql 测试
  - `--do-suite=villagesql` 运行所有测试包括子套件（insert、select、stored_procedure 等）
</Note>

### 运行单个测试

通过名称运行特定测试：

```bash
./mysql-test-run.pl villagesql.complex_index
```

从子套件运行测试：

```bash
./mysql-test-run.pl --suite=villagesql/insert
```

### 更新测试结果

如果您更改了功能需要更新预期测试输出：

```bash
./mysql-test-run.pl --record villagesql.complex_index
```

<Warning>
  **测试记录:** 始终检查 `.result` 文件的更改以确保反映预期行为而非错误。`--record` 标志会盲目覆盖预期输出。
</Warning>

### 测试可移植性

当测试输出包含测试运行器临时目录的路径时，请在 `.test` 文件内添加此指令，使记录结果不包含在其他机器上会失效的绝对路径：

```sql
--replace_result $MYSQLTEST_VARDIR MYSQLTEST_VARDIR
```

### 调试测试失败

VillageSQL 特定的日志消息（通过 `LogVSQL()` 发出）默认被抑制。要在测试运行期间在错误日志中显示它们：

```bash
./mysql-test-run.pl --mysqld=--log-error-verbosity=3 villagesql.complex_index
```

完整错误日志位于 `mysql-test/var/log/mysqld.1.err`。

有关编写扩展回归测试，请参阅 [开发指南](/mysql-8.4/0.0.4/development)。

## 安装二进制文件

要将 VillageSQL 系统级安装（需要适当权限）：

```bash
cmake --build build --target install
```

默认安装二进制文件到 `/usr/local/mysql/`。您可以自定义安装前缀：

```bash
cmake -S . -B build -DCMAKE_INSTALL_PREFIX=/opt/villagesql
cmake --build build --target install
```

## 贡献

如果您正在为 VillageSQL 贡献代码，请参阅 [贡献指南](https://github.com/villagesql/villagesql-server/blob/main/CONTRIBUTING.md) 了解编码标准和开发工作流程。