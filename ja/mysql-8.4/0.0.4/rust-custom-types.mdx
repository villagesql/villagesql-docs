---
title: "Rust でのカスタム型"
description: "VillageSQL Rust SDK で新しいカラム型を定義 — custom_type! マクロを使用してバイナリレイアウト、エンコード、デコード、比較、ハッシュ、算術関数を実装します。"
---

<Warning>
Rust SDK はバージョン 0.0.1 です。
</Warning>

カスタム型を使用すると、`RATIONAL`、`VECTOR`、`INET` などの新しいカラム型を定義でき、`ORDER BY`、インデックス、集約関数と連携できます。Rust SDK は `custom_type!` マクロを通じてこれをサポートしています。

このページでは、[Rust での拡張機能の構築](/mysql-8.4/0.0.4/rust-sdk)を既に完了していることを前提としています。設定（Cargo.toml、manifest.json、cargo-vsql）は同じです。

## カスタム型を使用するタイミング

以下のいずれかに当てはまる場合、カスタム型を使用します：

- 標準の SQL 型では表現できないバイナリディスクレイアウトが必要な場合（パックされた浮動小数点、固定幅整数、バイナリ識別子）
- 型固有の順序セマンティクスがあり、辞書順の文字列順序とは異なる場合
- サーバーが `ORDER BY`、`COUNT(DISTINCT)`、およびセット操作で値を正しくインデックス付けおよびハッシュ化できるようにしたい場合

SQL 呼び出し可能な関数のみが必要で、データが `STRING`、`INT`、または `REAL` カラムに適切に収まる場合は、カスタム型は必要ありません。

## custom_type! マクロ

すべてのカスタム型には 4 つのコールバック（エンコード、デコード、比較、ハッシュ）とデフォルト値が必要です。フルマクロシグネチャは次の通りです：

```rust
villagesql::custom_type!(
    type_name: "type_name_in_sql",
    persisted_length: N,
    max_decode_buffer_length: M,
    encode: your_encode_fn,
    decode: your_decode_fn,
    compare: your_compare_fn,
    hash: your_hash_fn,
    default: "a_valid_string_literal",
)
```

| Field | Type | Description |
|---|---|---|
| `type_name` | 文字列リテラル | SQL 型名。SQL では大文字小文字を区別しません。 |
| `persisted_length` | `usize` | ディスク上でのストレージに固定されたバイト長。 |
| `max_decode_buffer_length` | `usize` | 解読された文字列表現の最大バイト長。 |
| `encode` | fn | INSERT 時に `&str` をバイナリバイトに変換します。 |
| `decode` | fn | バイナリバイトを表示用に `String` に戻します。 |
| `compare` | fn | `ORDER BY`、`MIN`、`MAX` 用の `Ordering` を返します。 |
| `hash` | fn | `COUNT(DISTINCT)` およびセット操作用の `usize` ハッシュを返します。インデックス付きカラムにはオプションですが推奨されます。 |
| `default` | 文字列リテラル | タイプ初期化時にサーバーがエンコードできる有効な文字列。正確に `persisted_length` バイトにエンコードされる必要があります。オプションですが推奨されます。 |

`type_name`、`persisted_length`、`max_decode_buffer_length`、`encode`、`decode`、および `compare` は必須です。`hash` および `default` はオプションですが推奨されます — `hash` は正しい `COUNT(DISTINCT)` およびセット操作に必要で、`default` はタイプ初期化検証に必要です。

## バイナリ値の受信と返却

カスタム型を引数または戻り値として使用する関数は、生バイトを扱います。

**入力** — `InValue::Custom(b)` は格納されたバイナリを `&[u8]` として持ちます：

```rust
fn rational_numer_impl(args: &[InValue]) -> VdfReturn {
    match args.first() {
        Some(InValue::Custom(b)) => {
            let numer = read_i64(b, 0);
            VdfReturn::int(numer)
        }
        Some(InValue::Null) | None => VdfReturn::null(),
        _ => VdfReturn::error("rational_numer: expected a RATIONAL argument"),
    }
}
```

**出力** — `VdfReturn::Binary(bytes)` はバイナリバイトをサーバーに返します：

```rust
fn rational_add_impl(args: &[InValue]) -> VdfReturn {
    match (args.get(0), args.get(1)) {
        (Some(InValue::Custom(a)), Some(InValue::Custom(b))) => {
            let result = add_rationals(a, b);
            VdfReturn::Binary(result)
        }
        _ => VdfReturn::null(),
    }
}
```

カスタム型を `func!` 宣言で参照するには、`villagesql::custom!("type_name")` を使用します：

```rust
villagesql::func!(
    rational_add_impl,
    "rational_add",
    [villagesql::custom!("rational"), villagesql::custom!("rational")] -> villagesql::custom!("rational"),
    deterministic: true
)
```

## 例：有理数型

SDK リポジトリの `examples/vsql_rational` は、有理数型を実装する動作する拡張機能です。有理数をリトルエンディアンバイト順の 16 バイトの `i64` 値のペア（分子、分母）として格納し、算術関数を提供します。

以下のコア実装が含まれます：

```rust
use villagesql::{InValue, VdfReturn};

// Binary layout: [numerator: i64 LE][denominator: i64 LE] — 16 bytes total.
// Always stored in reduced form (GCD = 1) with a positive denominator.
const BYTES: usize = 16;

fn to_bytes(num: i64, den: i64) -> Vec<u8> {
    let mut v = Vec::with_capacity(BYTES);
    v.extend_from_slice(&num.to_le_bytes());
    v.extend_from_slice(&den.to_le_bytes());
    v
}

fn from_bytes(b: &[u8]) -> (i64, i64) {
    let num = i64::from_le_bytes(b[..8].try_into().unwrap());
    let den = i64::from_le_bytes(b[8..16].try_into().unwrap());
    (num, den)
}

// encode: "3/4" -> 16 bytes
pub fn rational_encode(s: &str) -> Result<Vec<u8>, String> {
    let (num_s, den_s) = s
        .split_once('/')
        .ok_or_else(|| format!("rational: expected 'n/d', got {:?}", s))?;
    let num: i64 = num_s.trim().parse()
        .map_err(|e| format!("rational numerator: {}", e))?;
    let den: i64 = den_s.trim().parse()
        .map_err(|e| format!("rational denominator: {}", e))?;
    let (n, d) = normalize(num as i128, den as i128)
        .ok_or_else(|| "rational: zero or overflowing denominator".to_string())?;
    Ok(to_bytes(n, d))
}

// decode: 16 bytes -> "3/4"
pub fn rational_decode(b: &[u8]) -> Result<String, String> {
    if b.len() < BYTES {
        return Err(format!("rational: expected {} bytes, got {}", BYTES, b.len()));
    }
    let (n, d) = from_bytes(b);
    Ok(format!("{}/{}", n, d))
}

// compare: for ORDER BY, MIN, MAX
pub fn rational_compare(a: &[u8], b: &[u8]) -> std::cmp::Ordering {
    let (n1, d1) = from_bytes(a);
    let (n2, d2) = from_bytes(b);
    // cross-multiply (denominators are always positive)
    let lhs = (n1 as i128) * (d2 as i128);
    let rhs = (n2 as i128) * (d1 as i128);
    lhs.cmp(&rhs)
}

// hash: for COUNT(DISTINCT) and set operations
pub fn rational_hash(b: &[u8]) -> usize {
    // FNV-1a over the 16 bytes
    let mut h: usize = 0xcbf29ce484222325u64 as usize;
    for &byte in b {
        h ^= byte as usize;
        h = h.wrapping_mul(0x100000001b3u64 as usize);
    }
    h
}
```

`custom_type!` 登録と算術 VDF（`rational_add`、`rational_sub` など）は、`examples/vsql_rational/src/lib.rs` で完全なソースを参照してください。

拡張機能をインストール後：

```sql
INSTALL EXTENSION 'vsql_rational';

CREATE TABLE fractions (
    id   INT PRIMARY KEY,
    val  RATIONAL
);

INSERT INTO fractions VALUES (1, '1/2'), (2, '3/4'), (3, '1/4');

-- ORDER BY uses rational_compare
SELECT val FROM fractions ORDER BY val;
-- → 1/4, 1/2, 3/4

-- Arithmetic with rational_add
SELECT rational_add('1/3', '1/6');
-- → 1/2

-- Extract numerator and denominator
SELECT rational_numer(val), rational_denom(val) FROM fractions;

-- Convert to floating-point approximation
SELECT rational_to_real('1/3');
-- → 0.3333333333333333
```

`rational_to_real(r RATIONAL) -> REAL` は分子を分母で割ることで `RATIONAL` 値を 64 ビット浮動小数点近似値に変換します。表示や比較に近似小数が必要だが、カラムに損失を伴う表現を保存したくない場合に有用です。

## types を含む extension! ブロック

関数と型の両方を登録する場合、`extension!` ブロックには 2 つのセクションがあります：

```rust
villagesql::extension! {
    funcs: [
        // VDFs declared with func!
    ],
    types: [
        // Custom types declared with custom_type!
    ]
}
```

空の場合はいずれのセクションも省略できます。型のみの拡張機能は `funcs:` を省略し、関数のみの拡張機能は `types:` を省略します。

## 次のステップ

<CardGroup cols={2}>
  <Card
    title="Rust API リファレンス"
    icon="book"
    href="/mysql-8.4/0.0.4/rust-api-reference"
  >
    InValue、VdfReturn、およびすべてのマクロの完全なリファレンス。
  </Card>
  <Card
    title="Rust での拡張機能の構築"
    icon="wrench"
    href="/mysql-8.4/0.0.4/rust-sdk"
  >
    はじめに — Cargo 設定、最初の関数、パッケージング、テスト。
  </Card>
  <Card
    title="C++ カスタム型"
    icon="shapes"
    href="/mysql-8.4/0.0.4/custom-types"
  >
    C++ でのカスタム型 — `make_type<>`、エンコード/デコード/比較/ハッシュ、ALTER TABLE ルール。
  </Card>
  <Card
    title="拡張機能アーキテクチャ"
    icon="sitemap"
    href="/mysql-8.4/0.0.4/architecture"
  >
    カスタム型の解決、キャッシュ、ストレージ方法。
  </Card>
</CardGroup>