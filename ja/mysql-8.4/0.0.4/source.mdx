---
title: "ソースからビルド"
description: "ソースコードからVillageSQL Server for MySQLをコンパイルし、拡張機能を活用して始めます。"
---

## 概要

VillageSQLをソースからビルドすると、最新の機能を入手でき、特定の環境に合わせてビルドをカスタマイズできます。

## 前提条件

開始する前に、以下のものをインストール済みであることを確認してください：

- **Git** - リポジトリをクローンするために
- **CMake** 3.16以降 - ビルドシステムジェネレータ
- **C++コンパイラ** - GCC 8+, Clang 8+, または MSVC 2019+
- **ビルドツール** - make、ninja、または同等のツール
- **開発ライブラリ** - OpenSSL、ncurses、pkg-config、bison、およびその他のMySQL依存関係

### 依存関係のインストール

**Ubuntu/Debian:**
```bash
sudo apt install cmake libssl-dev libncurses5-dev pkg-config bison \
                 libtirpc-dev rpcsvc-proto build-essential zlib1g-dev
```

**macOS (Homebrewを使用):**

まず、まだHomebrewをインストールしていない場合は、Homebrewをインストールしてください：
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

次に、依存関係をインストールします：
```bash
brew install cmake openssl pkgconf bison libtirpc rpcsvc-proto
```

## ステップ1: リポジトリをクローン

GitHubからVillageSQL Serverリポジトリをクローンします。ホームディレクトリにクローンして、以下のCMakeステップを変更なしで実行できるようにします：

```bash
cd $HOME
git clone --depth 1 https://github.com/villagesql/villagesql-server.git
cd villagesql-server
```

<Note>
  リポジトリはMySQLコードベースのため数GBに及びます。
</Note>

## ステップ2: CMakeで設定

リポジトリ外にビルドディレクトリを作成し、プロジェクトを設定します：

**Linux:**
```bash
# Create build directory (outside the repo)
mkdir -p $HOME/build/villagesql
cd $HOME/build/villagesql

# Configure with CMake
cmake $HOME/villagesql-server -DWITH_DEBUG=1 -DCMAKE_INSTALL_PREFIX=$HOME/mysql
```

**macOS:**
```bash
# Create build directory (outside the repo)
mkdir -p ~/build/villagesql
cd ~/build/villagesql

# Configure with CMake
cmake ~/villagesql-server -DWITH_DEBUG=1 -DCMAKE_INSTALL_PREFIX=~/mysql -DWITH_SSL=system
```

<Note>
  **Linuxユーザー:** 絶対パスには`$HOME`を使用します。**macOSユーザー:** `~`（チルダ）を使用します。リポジトリパスが異なる場合は、実際のクローン場所に置き換えてください。
</Note>

### CMakeオプションの説明

- `<path-to-repo>` - クローンされたVillageSQLリポジトリへのパス（Linuxでは`$HOME`、macOSでは`~`を使用）
- `-DWITH_DEBUG=1` - デバッグシンボルを有効にします（開発に推奨）
- `-DCMAKE_INSTALL_PREFIX=~/mysql` - インストールディレクトリを設定します
- `-DWITH_SSL=system` - システムのOpenSSLライブラリを使用します（macOSで必須）

### 追加のCMakeオプション

**デバッグシンボルなしのプロダクションビルド:**

Linux:
```bash
cmake $HOME/villagesql-server -DCMAKE_INSTALL_PREFIX=/usr/local/mysql
```

macOS:
```bash
cmake ~/villagesql-server -DCMAKE_INSTALL_PREFIX=/usr/local/mysql -DWITH_SSL=system
```

**カスタムコンパイルコメント付き:**

Linux:
```bash
cmake $HOME/villagesql-server -DWITH_DEBUG=1 \
      -DCMAKE_INSTALL_PREFIX=$HOME/mysql \
      -DCOMPILATION_COMMENT="VillageSQL Version of MySQL"
```

macOS:
```bash
cmake ~/villagesql-server -DWITH_DEBUG=1 \
      -DCMAKE_INSTALL_PREFIX=~/mysql \
      -DCOMPILATION_COMMENT="VillageSQL Version of MySQL"
```

**より厳格な警告を有効にした開発モード:**

Linux:
```bash
cmake $HOME/villagesql-server -DMYSQL_MAINTAINER_MODE=ON -DWITH_DEBUG=1
```

macOS:
```bash
cmake ~/villagesql-server -DMYSQL_MAINTAINER_MODE=ON -DWITH_DEBUG=1
```

<Note>
  再設定が必要な場合は、まずCMakeキャッシュをクリアしてください（ビルドディレクトリ内で実行）：
  ```bash
  rm CMakeCache.txt
  ```
</Note>

## ステップ3: コードをコンパイル

並列コンパイルを使用してmakeでVillageSQLをビルドします。ビルドディレクトリ内で実行します：

**サーバーのみをビルド（開発に推奨）:**
```bash
make -j10 mysqld
```

**すべてをビルド:**
```bash
make -j10
```

<Tip>
  CPUコア数に応じて並列度（`-j10`）を調整してください。システムの応答性を保つため、総コア数から2-4を引いてください。例：12コアマシンの場合、`-j10`を使用します。
</Tip>

完了後、サーバーバイナリがビルドされたことを確認します：

**Linux:**
```bash
ls $HOME/build/villagesql/bin/mysqld
```

**macOS:**
```bash
ls ~/build/villagesql/bin/mysqld
```

## ステップ4: データベースを初期化

サーバーを初めて起動する前に、データディレクトリを初期化します：

**Linux:**

プロダクション（生成されたパスワードを使用 - 推奨）:
```bash
mkdir -p $HOME/mysql-data/data
$HOME/build/villagesql/bin/mysqld --initialize --datadir=$HOME/mysql-data/data --basedir=$HOME/build/villagesql
```

開発（パスワードなし - オプション）:
```bash
mkdir -p $HOME/mysql-data/data
$HOME/build/villagesql/bin/mysqld --initialize-insecure --datadir=$HOME/mysql-data/data --basedir=$HOME/build/villagesql
```

**rootとして実行（Dockerまたはsudo）:**

rootとして実行（例：Docker）する場合、MySQLは`--user=root`フラグを必要とします：
```bash
# Initialize as root
$HOME/build/villagesql/bin/mysqld --user=root --initialize-insecure --datadir=$HOME/mysql-data/data --basedir=$HOME/build/villagesql
```

**macOS:**

プロダクション（生成されたパスワードを使用 - 推奨）:
```bash
mkdir -p ~/mysql-data/data
~/build/villagesql/bin/mysqld --initialize --datadir=~/mysql-data/data --basedir=~/build/villagesql
```

開発（パスワードなし - オプション）:
```bash
mkdir -p ~/mysql-data/data
~/build/villagesql/bin/mysqld --initialize-insecure --datadir=~/mysql-data/data --basedir=~/build/villagesql
```

<Note>
  プロダクションのような設定では`--initialize`（パスワードあり）を使用します。`--initialize-insecure`（パスワードなし）はローカル開発およびテストのみに使用してください。`--initialize`を使用する場合、一時パスワードが生成され、コンソールに表示されます：`A temporary password is generated for root@localhost: <password>`
</Note>

初期化が成功したことを確認するために、システムデータベースが作成されたことをチェックします：

**Linux:**
```bash
ls $HOME/mysql-data/data/mysql
```

**macOS:**
```bash
ls ~/mysql-data/data/mysql
```

## ステップ5: サーバーを起動

VillageSQLサーバーを起動します：

**Linux:**
```bash
$HOME/build/villagesql/bin/mysqld --gdb --datadir=$HOME/mysql-data/data --basedir=$HOME/build/villagesql
```

**rootとして実行（Dockerまたはsudo）:**
```bash
$HOME/build/villagesql/bin/mysqld --user=root --gdb --datadir=$HOME/mysql-data/data --basedir=$HOME/build/villagesql
```

**macOS:**
```bash
~/build/villagesql/bin/mysqld --gdb --datadir=~/mysql-data/data --basedir=~/build/villagesql
```

<Tip>
  `--gdb`フラグはCtrl-Cでサーバーをクリーンに停止できる`SIGINT`ハンドラをインストールします — ターミナルからインタラクティブに実行する際に有用です。バックグラウンドで実行するには、`mysqld`コマンドに`--daemonize`を追加します。
</Tip>

## ステップ6: MySQLクライアントで接続

新しいターミナルを開き、MySQLクライアントを使用してサーバーに接続します：

**Linux:**

--initialize-insecure（パスワードなし）を使用する場合:
```bash
$HOME/build/villagesql/bin/mysql -u root
```

--initialize（生成されたパスワードを使用）を使用する場合:
```bash
$HOME/build/villagesql/bin/mysql -u root -p
# Enter the temporary password printed during initialization
```

**macOS:**

--initialize-insecure（パスワードなし）を使用する場合:
```bash
~/build/villagesql/bin/mysql -u root
```

--initialize（生成されたパスワードを使用）を使用する場合:
```bash
~/build/villagesql/bin/mysql -u root -p
# Enter the temporary password printed during initialization
```

MySQLプロンプトが表示されるはずです：

```
Welcome to the VillageSQL Server for MySQL monitor.
Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

mysql>
```

### インストールの検証

VillageSQLが実行されていることを確認します：

```sql
SELECT VERSION();
```

開発ビルドにはバージョン文字列にgitコミットハッシュが含まれます：

```
8.4.9-villagesql-0.0.4
```

## ステップ7: ユーザーとデータベースの設定

### rootパスワードを変更

`--initialize`を使用した場合、一時パスワードを変更します：

```sql
SET PASSWORD = 'your-secure-password';
```

### 開発用ユーザーを作成

日常的な開発には、root以外のユーザーを作成します：

```sql
-- Create user
CREATE USER developer IDENTIFIED BY 'dev-password';

-- Grant all privileges
GRANT ALL PRIVILEGES ON *.* TO developer;
```

新しいユーザーとしてログアウトし、再接続します：

**Linux:**
```bash
# Ctrl-D to exit
$HOME/build/villagesql/bin/mysql -u developer -p
```

**macOS:**
```bash
# Ctrl-D to exit
~/build/villagesql/bin/mysql -u developer -p
```

### データベースを作成

```sql
CREATE DATABASE my_database;
USE my_database;
```

<Tip>
  特定のデータベースに接続: `mysql -u developer -p -D my_database`
</Tip>

GDBデバッグ、テスト実行、サーバーコードベースへの貢献については、[サーバー開発ガイド](/mysql-8.4/0.0.4/server-development)を参照してください。

## トラブルシューティング

### 依存関係が不足してビルドに失敗

プラットフォームに必要な開発パッケージをインストールしてください。エラーメッセージで特定の不足ライブラリを確認してください。

### サーバーが起動しない

- データディレクトリが初期化されたことを確認: `ls ~/mysql-data/data/`
- 他のMySQL/VillageSQLインスタンスがポート3306を使用していないか確認
- `~/mysql-data/data/*.err`のエラーログを確認

### 拡張機能のインストールに失敗

- 拡張機能ライブラリ（`.so`または`.dll`）がビルド出力に存在することを確認
- VillageSQLが拡張機能をロードするための必要な権限を持っていることを確認
- 拡張機能名および.vebファイル名が正しいことを確認

## 次にすること

<CardGroup cols={3}>
  <Card
    title="拡張機能の使用"
    icon="puzzle-piece"
    href="/mysql-8.4/0.0.4/install"
  >
    VillageSQLの拡張機能をインストール、更新、管理する方法を学びます。
  </Card>
  <Card
    title="拡張機能の作成"
    icon="code"
    href="/mysql-8.4/0.0.4/create"
  >
    VillageSQL用のカスタム拡張機能を構築します。
  </Card>
  <Card
    title="使い始め"
    icon="rocket"
    href="/mysql-8.4/0.0.4/index"
  >
    VillageSQLのクイックスタートガイド。
  </Card>
</CardGroup>