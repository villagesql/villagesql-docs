---
title: "vsql_uuid extension for MySQL"
sidebarTitle: "vsql_uuid"
description: "The vsql_uuid extension adds a native 16-byte uuid column type to MySQL and generates version 1, 3, 4, 5, 6, and 7 UUIDs in SQL."
keywords: "mysql uuid type, mysql uuid primary key, mysql uuid v7, mysql binary uuid, vsql_uuid"
audience: villagesql-user
---

{/* Generated by scripts/gen-extension-pages.py. Edit the body in
    snippets/extensions/vsql-uuid.mdx, and the title, description and
    See also list in scripts/extension-pages.json, then re-run it.
    See also is here, not in the snippet, because a snippet cannot take
    a property and a relative link fails mint broken-links. */}

import VsqlUuidPage from "/snippets/extensions/vsql-uuid.mdx";

<VsqlUuidPage />

## See also

- [UUIDs in MySQL](/guides/uuids) — what each version is for and which to choose
- [Primary key strategies](/guides/primary-key-strategies) — where a UUID key helps and where it costs
- [Install extensions](/mysql-9.7/stable/install) — how `INSTALL EXTENSION` works and where the server looks for a bundle
- [Available extensions](/mysql-9.7/stable/extensions) — the full catalog
- [villagesql/vsql-uuid](https://github.com/villagesql/vsql-uuid) — source, build instructions, and the known limitations
