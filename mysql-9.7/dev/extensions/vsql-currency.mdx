---
title: "vsql_currency extension for MySQL"
sidebarTitle: "vsql_currency"
description: "The vsql_currency extension adds a currency column type to MySQL that holds any of the 164 ISO 4217 codes in a single byte, case-insensitive on input."
keywords: "mysql currency type, iso 4217 mysql, mysql currency code column, vsql_currency"
audience: villagesql-user
---

{/* Generated by scripts/gen-extension-pages.py. Edit the body in
    snippets/extensions/vsql-currency.mdx, and the title, description and
    See also list in scripts/extension-pages.json, then re-run it.
    See also is here, not in the snippet, because a snippet cannot take
    a property and a relative link fails mint broken-links. */}

import VsqlCurrencyPage from "/snippets/extensions/vsql-currency.mdx";

<VsqlCurrencyPage />

## See also

- [Choosing data types](/guides/choosing-data-types) — when a narrower type earns its place
- [Install extensions](/mysql-9.7/dev/install) — how `INSTALL EXTENSION` works and where the server looks for a bundle
- [Available extensions](/mysql-9.7/dev/extensions) — the full catalog
- [villagesql/vsql-currency](https://github.com/villagesql/vsql-currency) — source, build instructions, and the known limitations
